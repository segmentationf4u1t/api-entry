refresh_rate: 30 seconds

appenders:
  file:
    kind: file
    path: "logs/app.log"
    encoder:
      pattern: "{d} - {l} - {m}{n}"

root:
  level: info
  appenders:
    - file

loggers:
  my_actix_api:
    level: info
    appenders:
      - file
    additive: false